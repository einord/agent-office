meta {
  name: agents remove
  type: http
  seq: 5
}

delete {
  url: {{url}}agents/test-1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
