meta {
  name: agents update
  type: http
  seq: 4
}

put {
  url: http://localhost:3100/agents/test-1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "activity": "working"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
