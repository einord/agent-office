# Agent Office CLI Configuration
# Copy this to .env and update values

# URL to your Agent Office backend server
# For local testing (backend on same machine): http://host.docker.internal:3100
# For production (backend on remote server): http://your-server:3100
AGENT_OFFICE_SERVER_URL=http://host.docker.internal:3100

# API key for authentication (must match a key in backend config.json)
AGENT_OFFICE_API_KEY=your-api-key-here
