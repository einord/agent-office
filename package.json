{
  "name": "agent-office",
  "private": true,
  "scripts": {
    "dev": "cd cli && pnpm dev",
    "build": "pnpm --filter agent-office build",
    "build:backend": "pnpm --filter agent-office-backend build",
    "start": "pnpm --filter agent-office start",
    "event": "./scripts/start-event.sh",
    "event:rebuild": "BUILD_BINARIES=always ./scripts/start-event.sh",
    "event:build": "cd cli && node scripts/build-event-binaries.mjs --skip-sign",
    "event:release": "cd cli && node scripts/build-event-binaries.mjs --notarize"
  },
  "version": "0.1.25",
  "dependencies": {
    "bonjour-service": "^1.3.0"
  }
}
